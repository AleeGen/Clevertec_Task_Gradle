plugins {
    id 'java'
}

version = '1.0'

repositories {
    mavenLocal()
    mavenCentral()
}

jar {
    manifest {
        attributes('Name': project.name, 'Version': project.version)
    }
}

dependencies {
    implementation 'ru.clevertec:Utils:1.3.5'
}

test {
    useJUnitPlatform()
}