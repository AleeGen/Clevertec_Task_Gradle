plugins {
    id 'java'
}

version = '1.0'

jar {
    manifest {
        attributes('Name': project.name, 'Version': project.version)
    }
}

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    implementation 'ru.clevertec:Utils:1.3.5'
}